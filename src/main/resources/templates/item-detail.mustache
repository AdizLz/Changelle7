<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}} - Subasta</title>
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 0;
        }
        .detail-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            padding: 50px;
            max-width: 900px;
            margin: 0 auto;
        }
        .item-image-placeholder {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            height: 400px;
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 5rem;
            margin-bottom: 30px;
        }
        .item-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 20px;
        }
        .item-price-tag {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }
        .item-description {
            font-size: 1.1rem;
            line-height: 1.8;
            color: #4a5568;
            margin-bottom: 30px;
            padding: 20px;
            background: #f7fafc;
            border-radius: 10px;
            border-left: 4px solid #667eea;
        }
        .offer-section {
            background: #f7fafc;
            padding: 30px;
            border-radius: 15px;
            margin-top: 30px;
        }
        .btn-offer {
            background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
            border: none;
            color: white;
            padding: 15px 40px;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 600;
            transition: all 0.3s ease;
            width: 100%;
        }
        .btn-offer:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 20px rgba(72, 187, 120, 0.4);
        }
        .form-control, .form-control:focus {
            border-radius: 10px;
            padding: 12px;
            border: 2px solid #e2e8f0;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        .btn-back {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }
        .btn-back:hover {
            color: #764ba2;
            transform: translateX(-5px);
        }
        #offer-form {
            animation: slideDown 0.3s ease;
        }
        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            }
            50% {
                transform: scale(1.1);
                background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
            }
        }
        .ws-status {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            margin-left: 15px;
        }
        .ws-connected {
            background: #d4edda;
            color: #155724;
        }
        .ws-disconnected {
            background: #f8d7da;
            color: #721c24;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="detail-container">
            <!-- Botón Volver -->
            <a href="/items" class="btn-back">
                <i class="bi bi-arrow-left-circle"></i> Volver a la lista
            </a>

            <!-- Imagen placeholder -->
            <div class="item-image-placeholder">
                <i class="bi bi-gem"></i>
            </div>

            <!-- Información del Item -->
            <h1 class="item-title">
                <i class="bi bi-star-fill text-warning"></i> {{name}}
            </h1>

            <div>
                <span class="item-price-tag" id="current-price" data-item-id="{{id}}">
                    <i class="bi bi-tag-fill"></i> {{price}}
                </span>
                <span class="ws-status ws-disconnected" id="ws-status">
                    <i class="bi bi-circle-fill"></i> Desconectado
                </span>
            </div>

            <div class="item-description">
                <i class="bi bi-info-circle-fill text-primary"></i>
                <strong>Descripción:</strong><br>
                {{description}}
            </div>

            <!-- Información de ofertas -->
            {{#offerCount}}
            <div class="alert alert-info">
                <i class="bi bi-people-fill"></i>
                <strong>{{offerCount}}</strong> persona(s) han hecho ofertas
                {{#highestOffer}}
                <br>
                <i class="bi bi-trophy-fill text-warning"></i>
                Oferta más alta: <strong>${{highestOffer}}</strong>
                {{/highestOffer}}
            </div>
            {{/offerCount}}

            <!-- Sección de Ofertas -->
            <div class="offer-section">
                <h3 class="mb-4">
                    <i class="bi bi-cash-coin"></i> ¿Te interesa este artículo?
                </h3>

                <button class="btn btn-offer offer-btn mb-3">
                    <i class="bi bi-hand-thumbs-up"></i> Hacer una Oferta
                </button>

                <!-- Formulario -->
                <form id="offer-form" style="display:none;">
                    <input type="hidden" id="item-id" value="{{id}}">

                    <div class="mb-3">
                        <label for="name-input" class="form-label">
                            <i class="bi bi-person-fill"></i> Tu nombre completo
                        </label>
                        <input type="text" class="form-control" id="name-input"
                               placeholder="Ej: Juan Pérez" required>
                    </div>

                    <div class="mb-3">
                        <label for="email-input" class="form-label">
                            <i class="bi bi-envelope-fill"></i> Tu correo electrónico
                        </label>
                        <input type="email" class="form-control" id="email-input"
                               placeholder="Ej: juan@example.com" required>
                    </div>

                    <div class="mb-3">
                        <label for="amount-input" class="form-label">
                            <i class="bi bi-currency-dollar"></i> Monto que ofreces (USD)
                        </label>
                        <input type="number" class="form-control" id="amount-input"
                               placeholder="Ej: 750.00" step="0.01" min="0.01" required>
                        <small class="text-muted">
                            <i class="bi bi-lightbulb"></i> Precio actual: {{price}}
                        </small>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success btn-lg">
                            <i class="bi bi-send-fill"></i> Enviar Oferta
                        </button>
                        <button type="button" class="btn btn-outline-secondary"
                                onclick="document.getElementById('offer-form').style.display='none'">
                            <i class="bi bi-x-circle"></i> Cancelar
                        </button>
                    </div>
                </form>
            </div>

            <!-- Información adicional -->
            <div class="alert alert-info mt-4" role="alert">
                <i class="bi bi-shield-check"></i>
                <strong>¡Compra segura!</strong> Todos nuestros artículos son auténticos y verificados.
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/script.js"></script>

    <!-- Estado del WebSocket (usar la conexión global si existe) -->
    <script>
        (function () {
            const statusElement = document.getElementById('ws-status');
            function setStatus(connected) {
                if (!statusElement) return;
                statusElement.className = connected ? 'ws-status ws-connected' : 'ws-status ws-disconnected';
                statusElement.innerHTML = '<i class="bi bi-circle-fill"></i> ' + (connected ? 'Conectado' : 'Desconectado');
            }
            function refreshStatus() {
                const ws = window.__priceWs;
                if (ws && ws.readyState === WebSocket.OPEN) setStatus(true);
                else setStatus(false);
            }
            function attachToWs() {
                const ws = window.__priceWs;
                if (!ws) return false;
                ws.addEventListener('open', refreshStatus);
                ws.addEventListener('close', refreshStatus);
                ws.addEventListener('error', refreshStatus);
                // Primer ajuste inmediato
                refreshStatus();
                return true;
            }
            // Intentar adjuntarse al WS global; si aún no existe, reintentar unos segundos
            if (!attachToWs()) {
                let attempts = 0;
                const timer = setInterval(() => {
                    if (attachToWs() || ++attempts > 10) clearInterval(timer);
                }, 500);
            }
        })();
    </script>

    <style>
        @keyframes slideInRight {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        @keyframes slideOutRight {
            from { transform: translateX(0); opacity: 1; }
            to { transform: translateX(100%); opacity: 0; }
        }
    </style>
</body>
</html>